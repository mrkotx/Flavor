<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Flavor" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:colorControlHighlight">@color/button_pressed</item>  <!-- Полупрозрачный при нажатии -->
        <item name="android:colorButtonNormal">?attr/colorPrimary</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Flavor" parent="Base.Theme.Flavor" />

</resources>